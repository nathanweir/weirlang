;; Fibonacci sequence
(defn fib (n)
  (if (<= n 1)
    n
    (+ (fib (- n 1)) (fib (- n 2)))))

(defn main ()
  (println (fib 0))
  (println (fib 1))
  (println (fib 2))
  (println (fib 5))
  (println (fib 10)))
